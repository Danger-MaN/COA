[functions]
  external_node_modules = ["@netlify/blobs"]
  node_bundler = "esbuild"

[build]
  command = "npm run build"
  publish = "dist"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
